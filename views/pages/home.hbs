
<div class="card-grid">
    {{#each pages}}
        <a href="{{this.path}}" class="home-card">
            <img src="{{this.image}}" alt="{{this.altDescription}}" class="card-img">
            <h2>{{this.title}}</h2>
            <p>{{this.description}}</p>
        </a>
    {{/each}}
</div>